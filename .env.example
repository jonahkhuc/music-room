# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/music_room

# YouTube Data API v3 key (optional – enables search; without it, paste URL only)
YOUTUBE_API_KEY=

# App URL (used for PWA / metadata)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Port (default 3000)
PORT=8000
